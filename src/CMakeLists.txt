ADD_EXECUTABLE(viennats     viennats.cpp)
IF(VIENNATS_STATIC_BUILD)
 TARGET_LINK_LIBRARIES(viennats ${LIBRARIES} ${Boost_LIBRARIES} "-static")
ELSE()
 TARGET_LINK_LIBRARIES(viennats ${LIBRARIES} ${Boost_LIBRARIES})
ENDIF(VIENNATS_STATIC_BUILD)
